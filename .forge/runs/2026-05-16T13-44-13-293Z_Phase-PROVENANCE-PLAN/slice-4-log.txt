
=== Slice 4: API surface — validation on POST, `capabilities` on /health, GET /memories/by-source ===
Worker: gh-copilot
Model: claude-sonnet-4.6
Started: 2026-05-16T13:49:51.547Z

=== STDOUT ===
● Read pforge-prompt-1778939395515-k2hc95.txt
  │ ~\AppData\Local\Temp\pforge-prompt-1778939395515-k2hc95.txt
  └ 29 lines read

✗ Read provenance.test.ts
  │ src\api\__tests__\provenance.test.ts
  └ Path does not exist

● Read routes.ts
  │ src\api\routes.ts
  └ 333 lines read

● List directory src\api
  └ 2 files found

● List directory src\api\__tests__
  └ 1 file found

● Read queries.ts
  │ src\db\queries.ts
  └ L1:50 (50 lines read)

● Read package.json
  └ 50 lines read

● Read routes.test.ts
  │ src\api\__tests__\routes.test.ts
  └ 281 lines read

● Read queries.ts
  │ src\db\queries.ts
  └ L50:120 (71 lines read)

● Read queries.ts
  │ src\db\queries.ts
  └ L120:250 (131 lines read)

● Read queries.ts
  │ src\db\queries.ts
  └ L250:350 (101 lines read)

● Read queries.ts
  │ src\db\queries.ts
  └ L350:420 (71 lines read)

Now I have a clear picture. Let me implement the three features:

1. **Validation on POST /memories** — validate `source` and `project` fields
2. **`capabilities` on /health** — add capabilities list to health response
3. **GET /memories/by-source** — new endpoint using existing `searchThoughtsBySource`

● Edit routes.ts +1
  └ src\api\routes.ts

Now add the capabilities to /health, validation on POST /memories, and the new GET /memories/by-source endpoint:

● Edit routes.ts +14 -1
  └ src\api\routes.ts

Now add source/project validation on POST /memories (after the supersedes check):

● Edit routes.ts +8
  └ src\api\routes.ts

Now add the GET /memories/by-source endpoint before the stats route:

● Edit routes.ts +45
  └ src\api\routes.ts

Now create the provenance test file:

● Create provenance.test.ts +266
  └ src\api\__tests__\provenance.test.ts

Now run the validation gate:

● Run provenance tests (shell)
  │ cd E:\GitHub\OpenBrain && npx vitest run
  │ src/api/__tests__/provenance.test.ts --reporter=dot 2>&1
  └ 94 lines...

All 16 tests pass. Let me also verify existing tests still pass:

● Run existing route tests (shell)
  │ cd E:\GitHub\OpenBrain && npx vitest run src/api/__tests__/routes.test.ts
  │ --reporter=dot 2>&1
  └ 57 lines...

All 15 existing tests still pass. All 16 new provenance tests pass.

**Summary of changes:**

- **`src/api/routes.ts`**: Added `searchThoughtsBySource` import, `capabilities` array to `/health`, source/project validation on `POST /memories`, and new `GET /memories/by-source` endpoint.
- **`src/api/__tests__/provenance.test.ts`**: Created with 16 tests covering all three features.

<!-- PFORGE_TRAJECTORY:BEGIN -->
I approached this slice by first reading the existing routes and query layer to understand the patterns already established. The codebase uses Hono with a clean route-per-feature pattern and consistent error handling. The `searchThoughtsBySource` query function already existed in `db/queries.ts`, so the GET endpoint was simply a matter of wiring it up with query parameter parsing and validation consistent with other routes.

For POST validation, I added guards for `source` and `project` after the existing `supersedes` check, matching the pattern of returning 400 with a descriptive error message. I checked both empty strings and whitespace-only strings since the existing content validation uses `trim().length`. The validation only triggers when the field is explicitly provided (not undefined), so omitting them still works as before.

The health endpoint capabilities list was straightforward — I enumerated all current route capabilities as short string tokens. Future slices adding new endpoints should remember to add their capability string here.

A gotcha worth noting: the existing `routes.test.ts` checks the health endpoint but only asserts `body.status`, so the new `capabilities` field didn't break it. However, if any test were doing a strict deep-equal on the health response, it would have broken. The test file `provenance.test.ts` mirrors the mock setup from `routes.test.ts` but adds `searchThoughtsBySource` to the mock. Key functions a later slice may need: `createApi` in `routes.ts`, the mock pattern in the test files, and `searchThoughtsBySource` in `queries.ts`.
<!-- PFORGE_TRAJECTORY:END -->



=== STDERR ===

Changes   +334 -1
AI Units  54.4 (1m 46s)
Tokens    ↑ 30.6k • ↓ 6.3k • 392.3k (cached)
