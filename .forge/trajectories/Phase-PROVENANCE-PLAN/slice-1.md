This was a confirm-and-validate pass. I read both migration files to verify their contents matched the spec, then ran the validation gate. The forward migration already contained the transaction wrapper, both generated stored columns extracting provenance hashes from the metadata JSONB, partial indexes with IF NOT EXISTS guards, and the match_thoughts_by_source RPC function with the correct signature and return type. The down migration had the correct reverse-order drops with IF EXISTS guards and the function signature matched exactly. No dead-ends or code changes were needed. Future slices that reference match_thoughts_by_source should note its four-parameter signature and that any changes to it must be mirrored in the down file's DROP FUNCTION call. The thoughts table's archived and project columns are dependencies that must exist from earlier migrations before this one runs.