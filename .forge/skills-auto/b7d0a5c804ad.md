---
sha256Prefix: b7d0a5c804ad
summary: "API surface — validation on POST, `capabilities` on /health, GET /memories/by-source"
createdAt: 2026-05-16T13:51:45.635Z
reuseCount: 0
contextSignature:
  sliceType: execute
  titleHash: 1c0655f8
  planBasename: Phase-PROVENANCE-PLAN
  domainKeywords: ["API endpoint design patterns"]
commands:
  - "npx vitest run src/api/__tests__/provenance.test.ts --reporter=dot && echo ok"
---

# Auto-skill: API surface — validation on POST, `capabilities` on /health, GET /memories/by-source

Captured by Plan-Forge Phase-25 auto-skill library (L2).
Reuse this recipe when a future slice matches the domain keywords above.

## Commands that worked

```
npx vitest run src/api/__tests__/provenance.test.ts --reporter=dot && echo ok
```
