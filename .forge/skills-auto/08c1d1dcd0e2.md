---
sha256Prefix: 08c1d1dcd0e2
summary: "Extend `queries.ts` — `ThoughtMetadata.provenance` + `searchThoughtsBySource`"
createdAt: 2026-05-16T13:49:51.258Z
reuseCount: 0
contextSignature:
  sliceType: execute
  titleHash: 371dea09
  planBasename: Phase-PROVENANCE-PLAN
  domainKeywords: []
commands:
  - "grep -q 'searchThoughtsBySource' src/db/queries.ts && grep -q 'provenance' src/db/queries.ts && npx tsc --noEmit && echo ok"
---

# Auto-skill: Extend `queries.ts` — `ThoughtMetadata.provenance` + `searchThoughtsBySource`

Captured by Plan-Forge Phase-25 auto-skill library (L2).
Reuse this recipe when a future slice matches the domain keywords above.

## Commands that worked

```
grep -q 'searchThoughtsBySource' src/db/queries.ts && grep -q 'provenance' src/db/queries.ts && npx tsc --noEmit && echo ok
```
